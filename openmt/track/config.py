"""Handling of config conversion for detectron2 backend."""

# TODO add detectron2 config conversion, look for what we can borrow from
#  detect here, maybe we can unify it in a conversion package or similar